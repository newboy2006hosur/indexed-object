{
  "name": "indexed-object",
  "version": "1.0.0",
  "description": "A class that indexes an array of objects by a specified key for easy access.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts",
    "version": "standard-version",
    "release": "npm publish",
    "ci": "npm run lint && npm run test && npm run build"
  },
  "keywords": [
    "indexed",
    "object",
    "array",
    "id",
    "getById",
    "typescript"
  ],
  "author": "Bolt",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "@typescript-eslint/parser": "^5.0.0",
    "eslint": "^8.0.0",
    "jest": "^27.4.3",
    "prettier": "^2.5.1",
    "standard-version": "^9.5.0",
    "ts-jest": "^27.1.0",
    "typescript": "^4.5.0"
  },
  "dependencies": {
    "lodash.sortby": "^4.2.0"
  },
  "standard-version": {
    "skip": {
      "commit": true,
      "tag": true
    }
  }
}
